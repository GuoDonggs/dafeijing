# -*- coding: utf-8 -*-
"""语音控制电脑的简易 Agent。

一条链路把「说话」变成「电脑上的动作」：

    麦克风 → 唤醒词 → 端点检测/录音 → 语音识别 → LLM 大脑（可调用工具）
                                                        ↓
    扬声器 ← 语音合成 ← 文本清洗 ←───────────────  工具执行结果

模块划分：
    config    配置与模型路径解析
    audio     麦克风 / 扬声器 IO
    speech    唤醒词 KWS、VAD 端点检测、ASR、TTS
    tools     电脑控制工具集（打开应用、音量、截图、系统信息、执行命令…）
    rules     离线意图路由（没有 API Key 也能用的兜底大脑）
    llm       OpenAI 兼容的 LLM 客户端（带工具调用）
    brain     大脑：LLM/规则 → 工具循环 → 一句可朗读的回复
    agent     状态机与主循环（唤醒 → 听 → 想 → 说，可打断）
"""

__version__ = "0.1.0"
__all__ = ["__version__"]
